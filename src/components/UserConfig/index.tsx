import { SetConfig } from './ConfigsDescription.'
import { ConfigHeading } from './SetConfig'

export const UserConfig = () => {
  return (
    <div>
      <ConfigHeading />
      <SetConfig />
    </div>
  )
}
